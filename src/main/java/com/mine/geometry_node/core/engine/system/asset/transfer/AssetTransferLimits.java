package com.mine.geometry_node.core.engine.system.asset.transfer;

/** Hard limits for one logical file payload. */
public final class AssetTransferLimits {
    public static final int MAX_FILE_BYTES = 64 * 1024 * 1024;

    private AssetTransferLimits() {
    }
}
