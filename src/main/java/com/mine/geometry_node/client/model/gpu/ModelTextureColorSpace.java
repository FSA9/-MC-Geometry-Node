package com.mine.geometry_node.client.model.gpu;

/** Decode and mip-filtering contract for one GPU texture projection. */
public enum ModelTextureColorSpace {
    SRGB_COLOR,
    LINEAR_DATA,
    NORMAL_VECTOR,
    SHADOW_OPACITY
}
