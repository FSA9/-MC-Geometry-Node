package com.mine.geometry_node.client.model.render.compat;

/** User-facing model integration policy. Degradation is a result, not a third mode. */
public enum ModelIntegrationMode {
    HOST_NATIVE,
    MODEL_TAKEOVER
}
